# React Admin Application

This is a React application with JSX support, built using Vite for fast development.

## 🚀 Getting Started

### Installation
All dependencies have been installed. If you need to reinstall:
```bash
npm install
```

### Development
Start the development server:
```bash
npm run dev
```
The app will open automatically at `http://localhost:3000`

### Build
Create a production build:
```bash
npm run build
```

### Preview Production Build
Preview the production build locally:
```bash
npm run preview
```

### Linting
Run ESLint to check code quality:
```bash
npm run lint
```

## 📦 Installed Dependencies

### Dependencies
- **react** (^18.2.0) - React library
- **react-dom** (^18.2.0) - React DOM rendering

### Dev Dependencies
- **vite** (^5.0.8) - Fast build tool and dev server
- **@vitejs/plugin-react** (^4.2.1) - Vite plugin for React with JSX support
- **eslint** (^8.55.0) - Code linting
- **eslint-plugin-react** (^7.33.2) - React-specific linting rules
- **eslint-plugin-react-hooks** (^4.6.0) - Hooks linting rules
- **eslint-plugin-react-refresh** (^0.4.5) - React Refresh linting

## 📁 Project Structure

```
Admin/
├── src/
│   ├── App.jsx          # Main App component
│   ├── App.css          # App styles
│   ├── main.jsx         # Entry point
│   └── index.css        # Global styles
├── index.html           # HTML template
├── vite.config.js       # Vite configuration
├── package.json         # Project dependencies
└── .gitignore          # Git ignore rules
```

## ✨ Features

- ⚡ **Vite** - Lightning fast HMR (Hot Module Replacement)
- ⚛️ **React 18** - Latest React features
- 🎨 **Modern Styling** - Glassmorphism effects and gradients
- 🔍 **ESLint** - Code quality and consistency
- 📱 **Responsive** - Mobile-friendly design

## 🎯 Next Steps

1. Start the dev server: `npm run dev`
2. Edit `src/App.jsx` to build your application
3. Add new components in the `src/` directory
4. Customize styles in CSS files

Happy coding! 🎉
